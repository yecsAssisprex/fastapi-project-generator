# --- Aplicacion ---
APP_NAME={{PROJECT_NAME}}
ENVIRONMENT=local
DEBUG=true
#{{ENV_DB}}

# --- Autenticacion ---
# El servicio NO emite tokens: valida localmente la firma de los que emite el
# servicio de identidad. El secreto tiene que ser el MISMO en ambos lados.
# Generar uno con:  openssl rand -base64 48
JWT_SECRET_KEY=
JWT_ALGORITHM=HS256
JWT_ISSUER=allegro-identity-auth
JWT_AUDIENCE=allegro-control-plane
