
# --- Postgres del docker-compose ---
# Dentro del compose la app alcanza la base por el nombre del servicio, `db`, no por
# localhost. Si cambias estas tres, cambia tambien DATABASE_URL de arriba.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB={{PROJECT_SNAKE}}
